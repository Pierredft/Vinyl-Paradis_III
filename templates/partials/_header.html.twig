<nav class="navbar navbar-expand-lg bgBeige dropShadow m15 p10">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{path("home.index")}}"><img src="./images/logovynile.png" class="logo" alt=""></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse posCenter" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{path("home.index")}}">Accueil</a>
                </li>
                <li class="nav-item">
                <a class="nav-link active" href="#">Nos Vinyles</a>
                </li>
                {# <li class="nav-item">
                    <a class="nav-link" href="#">Pricing</a>
                </li> #}
                {# <li class="nav-item">
                    <a class="nav-link disabled" aria-disabled="true">Disabled</a>
                </li> #}
                <li class="nav-item dropdown">
                    <a class="nav-link active dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fa-regular fa-user"></i>
                    </a>
                    <ul class="dropdown-menu">
                        {% if app.user %}
                        <li><a class="dropdown-item" href="#">Mon compte</a></li>
                        <li><a class="dropdown-item" href="{{path('app_logout')}}">Deconnexion</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a class="dropdown-item" href="{{path('admin')}}">Panel Admin</a></li>
                        {% endif %}
                        {% else %}
                            <li><a class="dropdown-item" href="{{path('app_login')}}">Connection</a></li>
                            <li><a class="dropdown-item" href="{{path('app_register')}}">Créer un compte</a></li>
                        {% endif %}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
<div class="message">
            {% if app.user %}
                <div>Bonjour
                    {{ app.user.name }}
                    {{ app.user.surname}}</div>
                <div class="ms-2">{{ app.user.email }}</div>
            {% endif %}
        </div>